# Custom Rules for Antigravity

- **No Unauthorized Deployments:** Never deploy any code/build changes (e.g. running `npm run deploy` or deploying via wrangler) unless the user explicitly instructs you to deploy in their prompt. Keep all changes strictly local until authorization is received.
